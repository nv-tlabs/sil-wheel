nuscenes-devkit>=1.1.10
requests>=2.28
tqdm
# vllm 0.20+ pins opencv-python-headless>=4.13 which pulls numpy>=2; nuscenes-devkit
# wants numpy<2. Forcing both ceilings gives a working combo (with a pip resolver
# warning that is safe to ignore).
numpy<2
opencv-python-headless<4.13
vllm>=0.6.0
qwen-vl-utils>=0.0.10
